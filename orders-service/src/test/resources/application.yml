customer:
  service:
    root:
      url: http://${DOCKER_HOST_IP}:8081/customers
    url: http://${DOCKER_HOST_IP}:8081/customers/{customerId}
datasources:
  default:
    url: jdbc:h2:mem:testdb
    username: sa
    password: test
    driverClassName: org.h2.Driver

eventuatelocal.kafka.bootstrap.servers: localhost:9092
